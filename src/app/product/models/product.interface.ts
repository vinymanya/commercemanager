// An interface is a virtual object that describe how the object looks like
// It's a descriptor of an object that we will be working with.
export interface Product {
  _id: string;
  name: string;
  quantity: number;
  price: number;
}
